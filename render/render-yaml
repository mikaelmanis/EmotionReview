services:
  - type: web
    name: emotion-model-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: HF_HOME
        value: /tmp/huggingface  # Prevents cache issues
